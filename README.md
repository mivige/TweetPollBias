# From Tweets to Ballots: Quantifying and Correcting Structural Biases in Social Media Polls through Post-Stratification

**Bachelor's Thesis - Sapienza University of Rome, Computer Science, 2026**

> Author: Michele Vincenzo Gentile

<a target="_blank" href="https://cookiecutter-data-science.drivendata.org/">
    <img src="https://img.shields.io/badge/CCDS-Project%20template-328F97?logo=cookiecutter" />
</a>

---

A multi-election social-poll analysis framework for Twitter. This project extracts bias markers (candidate ordering, appellatives, political leaning, sentiment, cognitive biases) and uses a **Sample-Calibrated Weighted GLM (SCWG)** to analyze how these factors correlate with poll outcomes across multiple election cycles (US 2016, 2020, 2024).

For setup, usage, and full documentation see the **[project wiki](https://github.com/mivige/TweetPollBias/wiki)**.

## Project Organization

```
├── LICENSE
├── Makefile           <- Convenience commands
├── README.md
├── data
│   ├── external       <- Data from third party sources.
│   ├── interim        <- Intermediate data that has been transformed.
│   ├── processed      <- Feature tables and model inputs generated by the pipeline.
│   └── raw            <- Original, immutable input data (not tracked by git).
│
├── docs               <- MkDocs documentation (mkdocs serve to browse locally).
│
├── notebooks          <- Jupyter notebooks for EDA and model evaluation.
│
├── pyproject.toml     <- Package metadata and tool configuration (ruff).
│
├── reports            <- Generated analysis outputs (HTML dashboards, JSON summaries).
│   └── figures        <- Generated plots and figures.
│
├── requirements.txt   <- Python dependencies.
│
└── bias_analysis      <- Main Python package.
    │
    ├── __init__.py
    ├── config.py               <- Paths, logging, and environment configuration.
    ├── dataset.py              <- Data loading utilities (JSONL, demographics, engagement).
    ├── election_configs.py     <- Per-election configuration registry.
    ├── features.py             <- Feature extraction pipeline (candidate order, appellatives, leaning, sentiment).
    ├── llm_bias_extraction.py  <- Cognitive bias detection via Gemini LLM.
    │
    ├── modeling
    │   ├── __init__.py
    │   └── train.py            <- GLM/OLS fitting and SCWG post-stratification.
    │
    └── plots.py                <- Visualizations and interactive HTML dashboard.
```
